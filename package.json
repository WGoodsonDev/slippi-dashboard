{
  "name": "slippi-dashboard",
  "version": "0.0.1",
  "description": "Web-based dashboard application for viewing derived data from Slippi replay files.",
  "license": "ISC",
  "author": "Warren Goodson",
  "repository": {
    "type": "git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\""
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
